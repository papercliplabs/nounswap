query NounsForAccountQuery($address: ID!) {
    account(id: $address) {
        # After 6000, hit skip limit (ok for now...)
        nouns(first: 6000) {
            id
            owner {
                id
            }
            seed {
                background
                body
                accessory
                head
                glasses
            }
        }
    }
}
