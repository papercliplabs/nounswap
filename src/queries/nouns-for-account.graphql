query NounsForAccountQuery($address: ID!) {
    account(id: $address) {
        nouns {
            id
            seed {
                background
                body
                accessory
                head
                glasses
            }
        }
    }
}
